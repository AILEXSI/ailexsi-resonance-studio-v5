# Mediabunny Removal Study

Investigation / controlled spike. **Not a removal.** Production default remains Mediabunny.

Results in this file are filled after the Chrome A/B harness and MODE A regression run. See `docs/compliance/mbr-evidence.json` once generated.

## Current Responsibility

Pending harness + inventory lock.

## Existing Native/HTML Alternative

`src/core/exporter/media.ts` — `loadVideo` / `seekVideo` / `requestVideoFrameCallback` / Canvas draw. Already the exporter fallback when `getDecoder` returns null.

## Experimental Backend

`setFrameSourceBackend("htmlvideo")` makes `getDecoder()` return null. `exportWithWebCodecs` then uses the existing HTML path. Default `"mediabunny"`. `resetFrameSourceBackend()` restores production.

## Test Media

Generated by `node scripts/generate-mbr-media.mjs` into `tests/fixtures/mbr/`. Frame-index barcode. Not third-party.

## Frame Accuracy Results

Pending Chrome harness.

## Random Seek Results

Pending Chrome harness.

## Resonance Semantics Results

Pending targeted vitest + harness.

## Performance Results

Pending Chrome harness.

## Environment Limitations

Pending.

## Windows Verification Required

Pending.

## Replacement Complexity

Pending.

## License/SBOM Impact

Pending. No package cleanup in this spike.

## Decision

Pending evidence.

## Evidence

- Initial main SHA: `a922c6eb77cb68494fbde9bd222a16b46ad00b32`
- Spike branch: `cursor/mediabunny-removal-study-ffe1`
- Harness: `scripts/mbr-frame-harness.html` + `scripts/mbr-run-chrome.mjs`
